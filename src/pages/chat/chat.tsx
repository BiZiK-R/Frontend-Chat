import React, { FC } from "react";
import cn from "classnames";

import "./chat.scss";

const Chat: FC = () => {
  return <img className={cn("Logo")} src="/image/logo.svg" />;
};

export default Chat;
